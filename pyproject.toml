[build-system]
requires = ["maturin>=1.4"]
build-backend = "maturin"

[project]
name = "minindex"
version = "0.1.0"
requires-python = ">=3.8"

[tool.maturin]
module-name = "minindex"
features = ["pyo3/extension-module"]
